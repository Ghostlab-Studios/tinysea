; TinySea API PHP settings
; .user.ini is respected by PHP-FPM (unlike php_value in .htaccess)
; These settings allow large CSV uploads (50-year scenarios are ~5 MB each)
post_max_size = 64M
upload_max_filesize = 64M
memory_limit = 256M
max_execution_time = 300
